python-magic>=0.4.27
PyYAML>=6.0.1
requests>=2.31.0
openai>=1.0.0
anthropic>=0.18.0
Pillow>=10.0.0
exifread>=3.0.0

# Note: binwalk is a system tool, install with:
# apt-get install binwalk (Debian/Ubuntu)
# brew install binwalk (macOS)
# The application will work without binwalk but that functionality will be skipped
